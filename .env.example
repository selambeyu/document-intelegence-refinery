# API keys (optional — only needed for query agent, vision VLM, or LLM section summaries)
# Set one of:
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...

# If OPENROUTER_API_KEY is set, vision and query agent use OpenRouter base URL.

# Local mode (no API key needed): USE_OLLAMA=1
# Optional: OLLAMA_MODEL=llama3.2
